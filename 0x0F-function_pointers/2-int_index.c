#include <stdio.h>
#include "function_pointers.h"
/**
 * int_index -  a function that searches for an integer.
 * @array: an array pointer
 * @size: size of array
 * @cmp: a integer pointer
 *
 * Return: ALways zero
 */
int int_index(int *array, int size, int (*cmp)(int))
{

	return (0);
}
