#include <stdio.h>
#include "main.h"
/**
 * add - Write a function that adds two
 * integers and returns the result.
 * @a: one of the numbers to be added
 * @b: The other number to be added to the first
 *
 * Return: Always 0 (Success)
 */
int add(int a, int b)
{
return (a + b);
}
