#include <stdio.h>
#include "main.h"
/**
 * _abs - Entry point
 * @num: take in the value to compute the absolute value
 *
 * Return: Always 0 (Success)
 */
int _abs(int num)
{
return ((num < 0) ? (-num) : num);
}
