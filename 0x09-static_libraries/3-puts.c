#include <stdio.h>
#include "main.h"
/**
 * _puts - Write a function that prints a string,
 * followed by a new line, to stdout.
 * @str: String
 *
 * Return: Always 0
 */
void _puts(char *str)
{
printf("%s\n", str);
}
