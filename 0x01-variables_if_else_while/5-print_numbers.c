#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int letter;
for (letter = 0; letter < 10; letter++)
{
printf("%d", letter);
}
printf("\n");
return (0);
}

